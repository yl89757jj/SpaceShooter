using UnityEngine;
using System.Collections;

public class DestoryEx : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 1f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
